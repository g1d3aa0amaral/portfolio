# -- Endereco do Listener
LISTENER =
 (DESCRIPTION_LIST =
   (DESCRIPTION =
     (ADDRESS = (PROTOCOL = TCP)(HOST = 169.254.***.***)(PORT = 1521))
   )
  )

# -- Servico do Gateway
SID_LIST_LISTENER =
 (SID_LIST =
   (SID_DESC =
     (SID_NAME = SQLSERVER)
     (ORACLE_HOME = C:\app\oracle\product\19.0.0\tghome_1) 
     (PROGRAM = dg4msql)
    )
  )
