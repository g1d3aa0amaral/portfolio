# -- Listener Address and Service (file on Gateway server - Windows VM - to receive Oracle request)
LISTENER =
 (DESCRIPTION_LIST =
   (DESCRIPTION =
     (ADDRESS = (PROTOCOL = TCP)(HOST = 169.254.***.***)(PORT = 1521))
   )
  )

SID_LIST_LISTENER =
 (SID_LIST =
   (SID_DESC =
     (SID_NAME = SQLSERVER)
     (ORACLE_HOME = C:\app\oracle\product\19.0.0\tghome_1) 
     (PROGRAM = dg4msql)
    )
  )
